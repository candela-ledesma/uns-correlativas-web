services:
  - type: web
    name: uns-parser-api
    runtime: python
    rootDir: .
    buildCommand: pip install -r parser_api/requirements.txt
    startCommand: python -m uvicorn parser_api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PARSER_API_SECRET
        sync: false
    plan: free
