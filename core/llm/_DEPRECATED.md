# DEPRECATED

Este módulo (`core/llm/`) está pendiente de borrar.

Contenía el adapter y normalizer del pipeline LLM viejo (pre-admin panel).
Reemplazado por el flujo directo PDF → Gemini API → JSON en `web/app/api/admin/planes/parsear/`.
